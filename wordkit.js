function reverse(str) {
  let newStr = '';
  if (str.length < 0 || str === " ") {
    return -1;
  } else {
    for (let i = str.length; i >=0; i--){
        newStr[i]
    }
  }
}
const string = 'Hello';
const value = reverse(string);
console.log(value);